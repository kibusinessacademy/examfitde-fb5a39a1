# RLS Audit — 2026-05-08

## 1. Permissive policies for `anon` role (33 total)
All reviewed and accepted as intentional. Categories:
- **Public catalog reads** (products, pricing_plans, product_*, professions, berufe, beruf_*, german_certification_master, cms_media, media_assets, package_tags, tag_groups, work_*, profession_*, oral_exam_session_templates, skill_nodes, step_dag_edges, seo_redirects, store_policy_flags) — required for unauthenticated SEO/landing pages.
- **Public write endpoints** (course_inquiries, pass_calculator_sessions, tracking_events) — INSERT-only, no SELECT exposure.

No action required. Whitelist documented; future additions require justification in PR description.

## 2. Tables with RLS enabled but no policy
- `growth_keyword_registry` — currently NO ACCESS for anon/authenticated (correct posture, internal/service_role only). Documented as intentional. Service-role policy is sufficient (bypasses RLS).

## 3. `service_role` policies with `USING true`
~80 service-role-only ALL policies. These are safe (service_role bypasses RLS regardless) and act as documentation of intent. No action.

## 4. `authenticated` SELECT true on potentially sensitive tables
- `azav_compliance_checks` / `azav_fachbereiche` / `beruf_market_data` — non-PII reference data, accepted.
