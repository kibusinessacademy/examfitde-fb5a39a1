name: Internal Link Hygiene Guard

on:
  workflow_dispatch:

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Run Internal Link Hygiene Guard
        run: node scripts/guards/internal-link-hygiene-guard.mjs
